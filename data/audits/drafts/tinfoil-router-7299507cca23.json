{
  "bundle_digest": "7299507cca23c4af0c289564778885d37d9c36dcf13eeb3fe74677b4639c5c91",
  "sigstore_measurement": "6dd91ce3a9b812c7b22f4fb0bd53c025c0b4fb6f4469a97f966ad50a0bc71496fb2723",
  "release_tag": "v0.0.142",
  "cvm_version": "0.11.0",
  "audited_at": "2026-09-02",
  "audited_by": ["agent-draft"],
  "verdict": "sigstore-tag-bound-baseline",
  "notes": "Tag mapping is DEFINITIVE (not presumptive): the live probe's bundle_digest 7299507c… equals v0.0.142's tinfoil.hash byte-for-byte, and the probe's sigstore_measurement 6dd91ce3… is the truncated prefix of v0.0.142's tinfoil-deployment.json snp_measurement (full: 6dd91ce3a9b812c7b22f4fb0bd53c025c0b4fb6f4469a97f966ad50a0bc71496fb2723ddabe96a6c79da746a64af129a). Router image ghcr.io/tinfoilsh/confidential-model-router@sha256:c51e12746b1747969… (v0.0.142). POSTURE — SOUND. The router is itself a measured, sigstore-tag-bound SEV-SNP enclave (verifiers/tinfoil.py re-verifies it: GH-OIDC SAN pinned to @refs/tags, snp_measurement == report, report_data binds SPKI+HPKE). It is a reverse-proxy gateway that DOES terminate client encryption and see prompt plaintext inside its enclave (reads bodies for token counting, billing, cache-salt, tool-runtime) — inherent to the gateway design; the trust is that this router code is attested. It does NOT substitute or expose backends unverified: manager/manager.go updateModelMeasurements() fetches each backend's latest tag→digest→sigstore bundle and calls sigstoreClient.VerifyAttestation(bundle, model.Repo, digest), where model.Repo is pinned by the router's embedded config.yml (a go:embed'd trust anchor whose sha256 is in the attested cmdline as tinfoil-config-hash). addEnclave() then fetches the backend's live attestation, Verify()s it, checks TDX hardware measurements, and REQUIRES verification.Measurement.Equals(model.SourceMeasurement) (commented 'SECURITY: This check is critical') before inserting; forwarded traffic is TLS-key-pinned via tinfoilClient.TLSBoundRoundTripper{ExpectedPublicKey: enclave.tlsKeyFP} from the attested cert FP. A malicious control plane cannot repoint a model to a different repo: sync() logs an error and ignores any remote config whose repo != the locally pinned repo ('Publish a new release and update this router if you want the change to take effect'). CONFIG SURFACE: container_count 1 ('proxy'); env_external = 1 (DOMAIN as a bare string; the other 5 env entries are attested key:value dicts); secrets_external = 5 (USAGE_REPORTER_SECRET, USAGE_CONTEXT_SECRET, INFERENCE_DELEGATION_SECRET, CACHE_ROUTE_SECRET, METRICS_API_KEY). So runtime_config_fully_attested is False, but benignly: the externalized values are operational (domain + billing/metrics/cache creds), none touch the repo-pin trust anchor, which is baked into the measurement. shim path allowlist: / /health /.well-known/tinfoil-proxy /.well-known/prometheus-targets /metrics /v1/*, authenticated. DIFF v0.0.141→v0.0.142 is MUNDANE: only Dockerfile (golang 1.26.5→1.26.6), go.mod/go.sum, and the release-populated config image placeholder changed — no routing/attestation-logic change. One nuance to watch: that bump moved tinfoil-go v0.15.2→v0.15.3, which swapped the SEV-SNP verifier lib from upstream google/go-sev-guest v0.15.0 to Tinfoil's fork tinfoilsh/go-sev-guest — a change in the library the router uses to re-attest backends, not in the router's own logic. First baseline in this ledger; no prior tinfoil row to diff against."
}
